<UserControl x:Class="Games_Store.Views.AdminPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Games_Store.ViewModels">
    <UserControl.DataContext>
        <vm:AdminViewModel />
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Admin Panel - Game Management"
                   FontSize="24" FontWeight="Bold" Foreground="#e94560"
                   Margin="0,0,0,15" />

        <!-- Search and Action Buttons -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource SearchBoxStyle}"
                     Tag="Search games by title..." />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                <Button Content="+ Add Game" Command="{Binding AddGameCommand}"
                        Style="{StaticResource AccentButtonStyle}" Margin="0,0,5,0" />
                <Button Content="Edit" Command="{Binding EditGameCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,5,0" />
                <Button Content="Delete" Command="{Binding DeleteGameCommand}"
                        Style="{StaticResource DangerButtonStyle}" Margin="0,0,5,0" />
                <Button Content="Toggle Featured" Command="{Binding ToggleFeaturedCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
        </Grid>

        <!-- Games DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Games}"
                  SelectedItem="{Binding SelectedGame}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" CanUserAddRows="False"
                  Background="#1a1a2e" Foreground="White"
                  BorderBrush="#2a2a4a" BorderThickness="1"
                  RowBackground="#1a1a2e" AlternatingRowBackground="#16213e"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#2a2a4a"
                  HeadersVisibility="Column"
                  ColumnHeaderHeight="40"
                  RowHeight="35">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#0f3460" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Padding" Value="10,5" />
                    <Setter Property="BorderBrush" Value="#2a2a4a" />
                    <Setter Property="BorderThickness" Value="0,0,1,0" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,5" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#e94560" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="100" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="80" />
                <DataGridCheckBoxColumn Header="Featured" Binding="{Binding IsFeatured}" Width="80" />
                <DataGridTextColumn Header="Image" Binding="{Binding ImageUrl}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Release Date" Binding="{Binding ReleaseDate, StringFormat=d}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Edit/Add Form (shown when IsEditing = true) -->
        <Border Grid.Row="3" Background="#16213e" CornerRadius="8" Padding="20" Margin="0,10,0,0"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="Title" Foreground="#aaa" Margin="0,0,0,4" />
                    <TextBox Text="{Binding FormTitle, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBoxStyle}" />
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,10">
                    <TextBlock Text="Genre" Foreground="#aaa" Margin="0,0,0,4" />
                    <TextBox Text="{Binding FormGenre, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBoxStyle}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="Price" Foreground="#aaa" Margin="0,0,0,4" />
                    <TextBox Text="{Binding FormPrice, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBoxStyle}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,10">
                    <TextBlock Text="Image" Foreground="#aaa" Margin="0,0,0,4" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding FormImageUrl, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormTextBoxStyle}" IsReadOnly="True" />
                        <Button Grid.Column="1" Content="Browse..."
                                Command="{Binding BrowseImageCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="6,0,0,0" Padding="12,6" FontSize="12" />
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,10">
                    <TextBlock Text="Description" Foreground="#aaa" Margin="0,0,0,4" />
                    <TextBox Text="{Binding FormDescription, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBoxStyle}"
                             Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" />
                </StackPanel>

                <CheckBox Grid.Row="3" Grid.ColumnSpan="2" Content="Featured Game"
                          IsChecked="{Binding FormIsFeatured}"
                          Foreground="White" Margin="0,0,0,15" />

                <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="Save" Command="{Binding SaveGameCommand}"
                            Style="{StaticResource AccentButtonStyle}" Margin="0,0,10,0" Width="100" />
                    <Button Content="Cancel" Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButtonStyle}" Width="100" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
